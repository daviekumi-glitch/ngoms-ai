# Firebase Configuration
VITE_FIREBASE_API_KEY=your_firebase_api_key_here
VITE_USE_FIREBASE=false

# Set VITE_USE_FIREBASE=true to use Firebase instead of Base44 API
# In the APK (Capacitor) build, Firebase is used automatically
